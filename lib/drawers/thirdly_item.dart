import 'package:flutter/material.dart';

class DrawerThirdItem extends StatelessWidget {
  const DrawerThirdItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      'Drawer Third Item'
    );
  }
}