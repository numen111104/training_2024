import 'package:flutter/material.dart';

class DrawerFirstItem extends StatelessWidget {
  const DrawerFirstItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      'Drawer Fist Item'
    );
  }
}