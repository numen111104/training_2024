

import 'package:flutter/material.dart';

class DrawerSecondItem extends StatelessWidget {
  const DrawerSecondItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Text(
      'Drawer Second Item'
    );
  }
}